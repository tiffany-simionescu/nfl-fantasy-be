// Router
const defensePlayerRouter = require('express').Router();

// Model
const DefensePlayers = require('../models/defensePlayer-model');

// Middleware


// == ENDPOINTS == //

// GET - /api/defenseplayers/fan/:id
// Gets all players saved by fan

// GET - /api/defenseplayers/:id

// POST - /api/defenseplayers
// Adds player to fan account

// PUT - /api/defenseplayers/:id
// To move player to a new team

// DELETE - /api/defenseplayers/:id
// Removes player from fan account